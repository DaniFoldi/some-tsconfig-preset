{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "some-tsconfig-preset/workers.json",
  "include": [
    "${configDir}/src/**/*",
    "${configDir}/e2e/**/*",
    "${configDir}/scripts/**/*",
    "${configDir}/test/**/*",
    "${configDir}/*.ts"
  ]
}
