{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "some-tsconfig-preset/base.json",
  "include": [
    "${configDir}/e2e/**/*",
    "${configDir}/scripts/**/*",
    "${configDir}/src/**/*",
    "${configDir}/types/**/*",
    "${configDir}/test/**/*",
    "${configDir}/*.ts"
  ]
}
