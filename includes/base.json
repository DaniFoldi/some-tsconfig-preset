{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../rulesets/base.json",
  "include": [
    "${configDir}/e2e",
    "${configDir}/scripts",
    "${configDir}/src",
    "${configDir}/test",
    "${configDir}/*.ts",
  ]
}
